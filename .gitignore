# Logs & runtime data
logs
*.log
npm-debug.log*
docker_context
.node_repl_history
pids
*.pid
*.seed

# Testing & coverage
lib-cov
coverage
.nyc_output
playwright-report/
test-results/
# Screenshots captured for PR review, attached to the PR rather than committed
pr-*.png

# Compiled outputs & binary addons (http://nodejs.org/api/addons.html)
build/Release
dist/
dll/
www/
*.apk
build/

# Dependency directories
node_modules
jspm_packages
plugins
platforms

# Caches
.npm
.tmp
Thumbs.db
docker_context
res/
.awcache
.DS_Store

# Secrets (keys, passwords, etc)
config.json
google-services.json
GoogleService-Info.plist
.env.local
.idea

# Claude Code: personal settings and session worktrees
.claude/settings.local.json
.claude/worktrees/

# Generated by `npm run compatibility:generate` from the simulation sources; the npm pre-hooks
# of start, build, test, sim and typecheck rebuild it.
src/data/RunCompatibility.json

# Generated by `npm run icons:generate` from public/images; the npm pre-hooks of start, build
# and test rebuild it. The service worker downloads the listed icons for offline play.
public/icons.json
